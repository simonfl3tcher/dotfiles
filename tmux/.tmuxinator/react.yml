# ~/.tmuxinator/react.yml

name: <%= @settings["workspace"] %>
root: ~/Documents/JS/React/<%= @settings["workspace"] %>

windows:
  - editor:
      layout: main-vertical
      panes:
        - v
  - tests:
      layout: main-vertical
      panes:
        - 
