# ~/.tmuxinator/vue.yml

name: <%= @settings["workspace"] %>
root: ~/Documents/JS/Vue/<%= @settings["workspace"] %>

windows:
  - editor:
      layout: main-vertical
      panes:
        - v
  - tests:
      layout: main-vertical
      panes:
        - 
